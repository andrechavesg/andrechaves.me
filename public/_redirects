# Trailing-slash consistency for the PT locale
/pt  /pt/  301

# Workers static assets: not_found_handling = "404-page" in wrangler.toml
# serves public/404.html with HTTP 404 for missing paths (incl. /assets/*).
# That 404 stops ES modules from treating HTML as JavaScript.
