# Trailing-slash consistency for the PT locale
/pt  /pt/  301

# NOTE: Cloudflare Pages _redirects does NOT support status 404.
# Missing paths (including /assets/*) fall back to /index.html with 200
# unless public/404.html exists — then Pages serves it with HTTP 404.
# That 404 is what stops ES modules from treating HTML as JavaScript.
