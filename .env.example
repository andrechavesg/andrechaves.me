# Optional — no secrets required for local build/deploy of the static site.
# Cloudflare credentials are provided via `wrangler login` or CI secrets, not committed files.
SITE_URL=https://andrechaves.me
